%% Generated by the Erlang ASN.1 compiler. Version: 5.3.1
%% Purpose: Erlang record definitions for each named and unnamed
%% SEQUENCE and SET, and macro definitions for each value
%% definition in module AddGrpB.

-ifndef(_ADDGRPB_HRL_).
-define(_ADDGRPB_HRL_, true).

-record('LatitudeDMS2', {
  d,
  m,
  s
}).

-record('LongitudeDMS2', {
  d,
  m,
  s
}).

-record('Node-LLdms-48b', {
  lon,
  lat
}).

-record('Node-LLdms-80b', {
  lon,
  lat
}).

-record('LaneDataAttribute-addGrpB', {
  %% with extension mark
}).

-record('MovementEvent-addGrpB', {
  startTime = asn1_NOVALUE,
  minEndTime,
  maxEndTime = asn1_NOVALUE,
  likelyTime = asn1_NOVALUE,
  confidence = asn1_NOVALUE,
  nextTime = asn1_NOVALUE
  %% with extension mark
}).

-record('Position3D-addGrpB', {
  latitude,
  longitude,
  elevation
  %% with extension mark
}).

-record('TimeMark', {
  year,
  month,
  day,
  summerTime,
  holiday,
  dayofWeek,
  hour,
  minute,
  second,
  tenthSecond
}).

-endif. %% _ADDGRPB_HRL_
