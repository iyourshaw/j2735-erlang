%% Generated by the Erlang ASN.1 compiler. Version: 5.3.1
%% Purpose: Erlang record definitions for each named and unnamed
%% SEQUENCE and SET, and macro definitions for each value
%% definition in module RoadUserChargingReportMessage.

-ifndef(_ROADUSERCHARGINGREPORTMESSAGE_HRL_).
-define(_ROADUSERCHARGINGREPORTMESSAGE_HRL_, true).

-record('RoadUserChargingReportMessage', {
  usageReport,
  serviceProviderData = asn1_NOVALUE
  %% with extension mark
}).

-record('FeeReport', {
  userData,
  fees
  %% with extension mark
}).

-record('UserData', {
  timeStamp,
  svcProvID,
  vehicleID,
  vehicleType,
  vehicleDescription = asn1_NOVALUE
  %% with extension mark
}).

-record('UserFeeInfo', {
  fee,
  timePeriod
  %% with extension mark
}).

-record('ItemizedPaymentReport', {
  timeStamp,
  timePeriod,
  svcProvID,
  chargerData
  %% with extension mark
}).

-record('ItemizedChargerData', {
  chargerID,
  chargingDescription = asn1_NOVALUE,
  configID = asn1_NOVALUE,
  tripInfo = asn1_NOVALUE,
  totalPaidToCharger
  %% with extension mark
}).

-record('TripInfo', {
  distanceTraveled = asn1_NOVALUE,
  distanceUnits = asn1_NOVALUE,
  timeTraveled = asn1_NOVALUE
  %% with extension mark
}).

-record('TimePeriod', {
  startTime,
  endTime
  %% with extension mark
}).

-endif. %% _ROADUSERCHARGINGREPORTMESSAGE_HRL_
