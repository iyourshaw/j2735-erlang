%% Generated by the Erlang ASN.1 compiler. Version: 5.3.1
%% Purpose: Erlang record definitions for each named and unnamed
%% SEQUENCE and SET, and macro definitions for each value
%% definition in module TollUsageMessage.

-ifndef(_TOLLUSAGEMESSAGE_HRL_).
-define(_TOLLUSAGEMESSAGE_HRL_, true).

-record('TollUsageMessage', {
  tollPointInfo,
  tempID,
  tumSequenceNum,
  tamSequenceNum,
  tumHash = asn1_NOVALUE,
  encryptedTumData
  %% with extension mark
}).

-record('TumData', {
  tollUserData,
  tollServiceProviderData = asn1_NOVALUE
  %% with extension mark
}).

-record('TollUserData', {
  timeStamp,
  tspId,
  vehicleId,
  vehType = asn1_NOVALUE,
  vehicleDescription = asn1_NOVALUE,
  vehAxlesAndWeight = asn1_NOVALUE,
  numOccupants = asn1_NOVALUE,
  entryTollPointId = asn1_NOVALUE,
  entryTimeStamp = asn1_NOVALUE,
  locAndTimeStamps = asn1_NOVALUE,
  lastTollPoints = asn1_NOVALUE,
  charge = asn1_NOVALUE
  %% with extension mark
}).

-record('VehicleAxlesAndWeightInfo', {
  vehNumAxles,
  vehWeightPerAxle,
  vehTotalWeight,
  vehWeightUnits
  %% with extension mark
}).

-record('VehicleId', {
  vehicleIdentity = asn1_NOVALUE,
  licPlateState = asn1_NOVALUE,
  licPlateNumVeh = asn1_NOVALUE,
  licPlateNumTrailer = asn1_NOVALUE,
  userId = asn1_NOVALUE
  %% with extension mark
}).

-record('LocAndTimeStamp', {
  latitude,
  longitude,
  elevation = asn1_NOVALUE,
  timeStamp,
  currentLane = asn1_NOVALUE
  %% with extension mark
}).

-record('LastTollPointInfo', {
  tollChargerInfo,
  timeStamp
  %% with extension mark
}).

-endif. %% _TOLLUSAGEMESSAGE_HRL_
