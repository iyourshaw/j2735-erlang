%% Generated by the Erlang ASN.1 compiler. Version: 5.3.1
%% Purpose: Erlang record definitions for each named and unnamed
%% SEQUENCE and SET, and macro definitions for each value
%% definition in module ManeuverSharingAndCoordinatingMessage.

-ifndef(_MANEUVERSHARINGANDCOORDINATINGMESSAGE_HRL_).
-define(_MANEUVERSHARINGANDCOORDINATINGMESSAGE_HRL_, true).

-record('ManeuverSharingAndCoordinatingMessage', {
  mSCMTimestamp,
  hVPos = asn1_NOVALUE,
  hVPosXYConf = asn1_NOVALUE,
  mSCMType,
  sourceID,
  destinationIDs = asn1_NOVALUE,
  maneuverID = asn1_NOVALUE,
  maneuver = asn1_NOVALUE,
  responseFlag = asn1_NOVALUE,
  reasonCode = asn1_NOVALUE,
  maneuverExecutionStatus = asn1_NOVALUE
  %% with extension mark
}).

-record('Maneuver', {
  subManeuverList
  %% with extension mark
}).

-record('SubManeuver', {
  executantVehID,
  affectedVehIDs,
  currentStateData,
  targetRoadResource = asn1_NOVALUE,
  startTime,
  endTime,
  minSpeed = asn1_NOVALUE,
  maxSpeed = asn1_NOVALUE
  %% with extension mark
}).

-record('CurrentStateData', {
  executantVehPos = asn1_NOVALUE,
  executantVehPosConfidence = asn1_NOVALUE,
  speed,
  heading,
  accel4way = asn1_NOVALUE,
  size
  %% with extension mark
}).

-record('PositionOffsetXYZ', {
  offsetX,
  offsetY,
  offsetZ = asn1_NOVALUE
}).

-record('TargetRoadResource', {
  tRRType,
  tRRLocation
  %% with extension mark
}).

-record('TRRLocationType1', {
  tRRLength,
  frontSidePosition,
  laneOffset,
  tRRMovement = asn1_NOVALUE
  %% with extension mark
}).

-record('TRRLocationType2', {
  safetyGap,
  frontSideVehicle = asn1_NOVALUE,
  rearSideVehicle = asn1_NOVALUE,
  tRRLength = asn1_NOVALUE,
  laneOffset
  %% with extension mark
}).

-record('SurroundingVehicleInfo', {
  surroundingVehicleID,
  surroundingVehiclePosition,
  surroundingVehicleSize
  %% with extension mark
}).

-endif. %% _MANEUVERSHARINGANDCOORDINATINGMESSAGE_HRL_
