%% Generated by the Erlang ASN.1 compiler. Version: 5.3.1
%% Purpose: Erlang record definitions for each named and unnamed
%% SEQUENCE and SET, and macro definitions for each value
%% definition in module SignalStatusMessage.

-ifndef(_SIGNALSTATUSMESSAGE_HRL_).
-define(_SIGNALSTATUSMESSAGE_HRL_, true).

-record('SignalStatusMessage', {
  timeStamp = asn1_NOVALUE,
  second,
  sequenceNumber = asn1_NOVALUE,
  status,
  regional = asn1_NOVALUE
  %% with extension mark
}).

-record('SignalStatusMessage_regional_SEQOF', {
  regionId,
  regExtValue
}).

-record('SignalRequesterInfo', {
  id,
  request,
  sequenceNumber,
  role = asn1_NOVALUE,
  typeData = asn1_NOVALUE
  %% with extension mark
}).

-record('SignalStatusPackage', {
  requester = asn1_NOVALUE,
  inboundOn,
  outboundOn = asn1_NOVALUE,
  minute = asn1_NOVALUE,
  second = asn1_NOVALUE,
  duration = asn1_NOVALUE,
  status,
  regional = asn1_NOVALUE
  %% with extension mark
}).

-record('SignalStatusPackage_regional_SEQOF', {
  regionId,
  regExtValue
}).

-record('SignalStatus', {
  sequenceNumber,
  id,
  sigStatus,
  regional = asn1_NOVALUE
  %% with extension mark
}).

-record('SignalStatus_regional_SEQOF', {
  regionId,
  regExtValue
}).

-endif. %% _SIGNALSTATUSMESSAGE_HRL_
