%% Generated by the Erlang ASN.1 compiler. Version: 5.3.1
%% Purpose: Erlang record definitions for each named and unnamed
%% SEQUENCE and SET, and macro definitions for each value
%% definition in module TravelerInformation.

-ifndef(_TRAVELERINFORMATION_HRL_).
-define(_TRAVELERINFORMATION_HRL_, true).

-record('TravelerInformation', {
  msgCnt,
  timeStamp = asn1_NOVALUE,
  packetID = asn1_NOVALUE,
  urlB = asn1_NOVALUE,
  dataFrames,
  regional = asn1_NOVALUE
  %% with extension mark
}).

-record('TravelerInformation_regional_SEQOF', {
  regionId,
  regExtValue
}).

-record('Circle', {
  center,
  radius,
  units
}).

-record('GeographicalPath', {
  name = asn1_NOVALUE,
  id = asn1_NOVALUE,
  anchor = asn1_NOVALUE,
  laneWidth = asn1_NOVALUE,
  directionality = asn1_NOVALUE,
  closedPath = asn1_NOVALUE,
  direction = asn1_NOVALUE,
  description = asn1_NOVALUE,
  regional = asn1_NOVALUE
  %% with extension mark
}).

-record('GeographicalPath_regional_SEQOF', {
  regionId,
  regExtValue
}).

-record('GeometricProjection', {
  direction,
  extent = asn1_NOVALUE,
  laneWidth = asn1_NOVALUE,
  circle,
  regional = asn1_NOVALUE
  %% with extension mark
}).

-record('GeometricProjection_regional_SEQOF', {
  regionId,
  regExtValue
}).

-record('ExitService_SEQOF', {
  item
}).

-record('GenericSignage_SEQOF', {
  item
}).

-record('SpeedLimit_SEQOF', {
  item
}).

-record('WorkZone_SEQOF', {
  item
}).

-record('Node-LL-24B', {
  lon,
  lat
}).

-record('Node-LL-28B', {
  lon,
  lat
}).

-record('Node-LL-32B', {
  lon,
  lat
}).

-record('Node-LL-36B', {
  lon,
  lat
}).

-record('Node-LL-44B', {
  lon,
  lat
}).

-record('Node-LL-48B', {
  lon,
  lat
}).

-record('NodeAttributeSetLL', {
  localNode = asn1_NOVALUE,
  disabled = asn1_NOVALUE,
  enabled = asn1_NOVALUE,
  data = asn1_NOVALUE,
  dWidth = asn1_NOVALUE,
  dElevation = asn1_NOVALUE,
  regional = asn1_NOVALUE
  %% with extension mark
}).

-record('NodeAttributeSetLL_regional_SEQOF', {
  regionId,
  regExtValue
}).

-record('NodeLL', {
  delta,
  attributes = asn1_NOVALUE
  %% with extension mark
}).

-record('NodeOffsetPointLL_regional', {
  regionId,
  regExtValue
}).

-record('OffsetSystem', {
  scale = asn1_NOVALUE,
  offset
}).

-record('RegionOffsets', {
  xOffset,
  yOffset,
  zOffset = asn1_NOVALUE
}).

-record('RegionPointSet', {
  anchor = asn1_NOVALUE,
  scale = asn1_NOVALUE,
  nodeList
  %% with extension mark
}).

-record('RoadSignID', {
  position,
  viewAngle,
  mutcdCode = asn1_NOVALUE,
  crc = asn1_NOVALUE
}).

-record('ShapePointSet', {
  anchor = asn1_NOVALUE,
  laneWidth = asn1_NOVALUE,
  directionality = asn1_NOVALUE,
  nodeList
  %% with extension mark
}).

-record('TravelerDataFrame', {
  doNotUse1,
  frameType,
  msgId,
  startYear = asn1_NOVALUE,
  startTime,
  durationTime,
  priority,
  doNotUse2,
  regions,
  doNotUse3,
  doNotUse4,
  content,
  url = asn1_NOVALUE,
  %% with extensions
  contentNew
}).

-record('ValidRegion', {
  direction,
  extent = asn1_NOVALUE,
  area
}).

-endif. %% _TRAVELERINFORMATION_HRL_
