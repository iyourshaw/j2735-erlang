%% Generated by the Erlang ASN.1 compiler. Version: 5.3.1
%% Purpose: Erlang record definitions for each named and unnamed
%% SEQUENCE and SET, and macro definitions for each value
%% definition in module ProbeDataReport.

-ifndef(_PROBEDATAREPORT_HRL_).
-define(_PROBEDATAREPORT_HRL_, true).

-record('ProbeDataReportMessage', {
  configId,
  reportChar,
  roadAuthorityID = asn1_NOVALUE,
  instantaneousRecs = asn1_NOVALUE,
  avgAndSumRec = asn1_NOVALUE
  %% with extension mark
}).

-record('ReportCharacteristics', {
  timeStamp,
  position,
  posAccuracy,
  velocity,
  vehicleClass = asn1_NOVALUE,
  devType = asn1_NOVALUE,
  vehicleSize = asn1_NOVALUE,
  vehicleMass = asn1_NOVALUE,
  vehPassengerCount = asn1_NOVALUE
  %% with extension mark
}).

-record('RptDevType', {
  oemOrAftermarket,
  canOrIvnConnected,
  mcoCapable = asn1_NOVALUE
  %% with extension mark
}).

-record('RptInstantaneousRecord', {
  timeOfInstantRec,
  locOfInstantRec,
  accOfPosition,
  velOfInstantRec,
  record = asn1_NOVALUE
  %% with extension mark
}).

-record('RptVelocity', {
  speed,
  speedStdDev = asn1_NOVALUE,
  heading = asn1_NOVALUE
  %% with extension mark
}).

-record('RptAveragedAndSummaryRecord', {
  startTime,
  startLoc,
  startAccOfPos,
  endTime,
  endLoc,
  endAccOfPos,
  record
  %% with extension mark
}).

-record('RptIntervalEvents', {
  wiperStatus = asn1_NOVALUE,
  vehCount = asn1_NOVALUE,
  weatherData = asn1_NOVALUE,
  transitVehData = asn1_NOVALUE,
  numOfOccupants = asn1_NOVALUE
  %% with extension mark
}).

-record('RptTransitVehData', {
  currNumPasngers = asn1_NOVALUE,
  avgNumPasngers = asn1_NOVALUE,
  trnstVehSchAdh = asn1_NOVALUE
  %% with extension mark
}).

-record('RptKinematicEvents', {
  events,
  hysteresis = asn1_NOVALUE
  %% with extension mark
}).

-record('RptHysteresis', {
  time,
  position,
  positionalAccuracy = asn1_NOVALUE,
  velocity
  %% with extension mark
}).

-record('RptRoadRoughness', {
  meanVerticalVariation,
  verticalVariationStdDev = asn1_NOVALUE,
  meanHorizontalVariation = asn1_NOVALUE,
  horizontalVariationStdDev = asn1_NOVALUE
  %% with extension mark
}).

-record('RptTrafficSigEncounters', {
  intersectionID = asn1_NOVALUE,
  trafficMetrics
  %% with extension mark
}).

-record('RptAveragedRecord', {
  avgSpeed = asn1_NOVALUE,
  speedStdDev = asn1_NOVALUE,
  avgStopDuration = asn1_NOVALUE,
  avgStopStdDev = asn1_NOVALUE,
  avgFuelConsumption = asn1_NOVALUE,
  avgEmissions = asn1_NOVALUE,
  avgNumOfOccupants = asn1_NOVALUE
  %% with extension mark
}).

-record('RptSummaryRecord', {
  regTravelTime = asn1_NOVALUE,
  regVehDistTraveled = asn1_NOVALUE,
  regVehTimeTraveled = asn1_NOVALUE,
  fuelConsumption = asn1_NOVALUE,
  totalVehEmissions = asn1_NOVALUE,
  numOfLowSpeedEvents = asn1_NOVALUE,
  timeStopped = asn1_NOVALUE,
  numOfStopped = asn1_NOVALUE,
  locOfStops = asn1_NOVALUE,
  regNumOfVehPassed = asn1_NOVALUE,
  regNumOfSurpassedVeh = asn1_NOVALUE,
  totalMsgsReceived = asn1_NOVALUE
  %% with extension mark
}).

-record('RptEmissions', {
  hydrocarbons = asn1_NOVALUE,
  co = asn1_NOVALUE,
  co2 = asn1_NOVALUE,
  no = asn1_NOVALUE,
  no2 = asn1_NOVALUE,
  so2 = asn1_NOVALUE,
  o3 = asn1_NOVALUE,
  pm10 = asn1_NOVALUE,
  pm25 = asn1_NOVALUE
  %% with extension mark
}).

-record('RptLocOfStops', {
  locationOfStop,
  locAccOfStop,
  durationOfStop
  %% with extension mark
}).

-endif. %% _PROBEDATAREPORT_HRL_
