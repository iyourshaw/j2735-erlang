%% Generated by the Erlang ASN.1 compiler. Version: 5.3.1
%% Purpose: Erlang record definitions for each named and unnamed
%% SEQUENCE and SET, and macro definitions for each value
%% definition in module TrafficLightStatusMessage.

-ifndef(_TRAFFICLIGHTSTATUSMESSAGE_HRL_).
-define(_TRAFFICLIGHTSTATUSMESSAGE_HRL_, true).

-record('TrafficLightStatusMessage', {
  trafficLightID,
  long = asn1_NOVALUE,
  lat = asn1_NOVALUE,
  operationStatus,
  controllerStatus,
  cyclecounter,
  cycletime,
  currentTime,
  trafficLightStatus
  %% with extension mark
}).

-record('TrafficLightStatus', {
  trafficLightType,
  consecutiveTrafficLight,
  trafficLightIntervalType,
  pedestrianCall,
  actuatedinterval,
  permissiveNonProtected,
  lightingStatus,
  maxIntervalLength,
  remainingTime,
  directionCode,
  reserved
  %% with extension mark
}).

-endif. %% _TRAFFICLIGHTSTATUSMESSAGE_HRL_
