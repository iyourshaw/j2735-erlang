%% Generated by the Erlang ASN.1 compiler. Version: 5.3.1
%% Purpose: Erlang record definitions for each named and unnamed
%% SEQUENCE and SET, and macro definitions for each value
%% definition in module TollAdvertisementMessage.

-ifndef(_TOLLADVERTISEMENTMESSAGE_HRL_).
-define(_TOLLADVERTISEMENTMESSAGE_HRL_, true).

-record('TollAdvertisementMessage', {
  tollChargesTable,
  tollAdvInfo = asn1_NOVALUE
  %% with extension mark
}).

-record('TollAdvertisementInfo', {
  tamSequenceNum,
  totalTams,
  tamNum,
  tollChargerInfo,
  timestamp,
  tumInstructions = asn1_NOVALUE,
  tollPointMap,
  tumPublicKey,
  ackPolicy,
  lastTollPtDataReq = asn1_NOVALUE
  %% with extension mark
}).

-record('TumInstructions', {
  maxNumOfLocTimeStamps,
  locTimeStampRate
  %% with extension mark
}).

-record('AckPolicy', {
  timeout,
  numOfRetries
  %% with extension mark
}).

-record('TollChargesTable', {
  tollTypeCharge
  %% with extension mark
}).

-record('LaneChargesTable', {
  laneId,
  chargesTable
  %% with extension mark
}).

-record('EntryChargesTable', {
  exitInfo,
  chargesTable
  %% with extension mark
}).

-record('ExitChargesTable', {
  entryInfo = asn1_NOVALUE,
  chargesTable
  %% with extension mark
}).

-record('ExitInfo', {
  exitNumber,
  exitLetter = asn1_NOVALUE,
  tollPointID = asn1_NOVALUE
  %% with extension mark
}).

-record('TimeChargesTable', {
  maxTime,
  chargesTable
  %% with extension mark
}).

-record('VehTypeCharges', {
  vehType,
  charges,
  specialCharges = asn1_NOVALUE
  %% with extension mark
}).

-record('SpecialCharges', {
  hov2Charge = asn1_NOVALUE,
  hov3Charge = asn1_NOVALUE,
  hov4Charge = asn1_NOVALUE,
  hov5PlusCharge = asn1_NOVALUE,
  cleanAirCharge = asn1_NOVALUE,
  operatorDefined = asn1_NOVALUE,
  engineCharacteristics = asn1_NOVALUE
  %% with extension mark
}).

-record('OperatorDefined', {
  opDefCharge1 = asn1_NOVALUE,
  opDefCharge2 = asn1_NOVALUE,
  opDefCharge3 = asn1_NOVALUE,
  opDefCharge4 = asn1_NOVALUE,
  opDefCharge5 = asn1_NOVALUE,
  opDefCharge6 = asn1_NOVALUE,
  opDefCharge7 = asn1_NOVALUE,
  opDefCharge8 = asn1_NOVALUE,
  opDefCharge9 = asn1_NOVALUE,
  opDefCharge10 = asn1_NOVALUE,
  opDefCharge11 = asn1_NOVALUE,
  opDefCharge12 = asn1_NOVALUE,
  opDefCharge13 = asn1_NOVALUE,
  opDefCharge14 = asn1_NOVALUE,
  opDefCharge15 = asn1_NOVALUE,
  opDefCharge16 = asn1_NOVALUE
  %% with extension mark
}).

-record('TotalWeightCharges', {
  weightLimit,
  weightLimitUnits,
  weightCharge
  %% with extension mark
}).

-record('PerAxleWeightCharges', {
  totalWeightLimit,
  maxLadenWeightOnAxle,
  weightLimitUnits,
  axleWeightCharge
  %% with extension mark
}).

-record('AxlesCharges', {
  axlesLimit,
  axlesCharge
  %% with extension mark
}).

-record('TollPointMap', {
  revisionNum,
  referencePoint,
  speedLimit,
  laneWidth,
  approachLanesMap,
  tollZoneLanesMap
  %% with extension mark
}).

-record('TollChargerInfo', {
  tollChargerId,
  tollPointId,
  descriptiveName = asn1_NOVALUE
  %% with extension mark
}).

-endif. %% _TOLLADVERTISEMENTMESSAGE_HRL_
